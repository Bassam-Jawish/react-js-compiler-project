
import React, { useEffect, useState } from "react";

const [name, setName] = useState(""); // Error: Hook: (useState("");) is not inside a function component at the top level

function MyComponent({ items, setItems }) {

    const [searchTerm, setSearchTerm] = useState("");

    if (true) {
         useEffect(() => {

          }, [searchTerm]); // Error: Hook: (useEffect(()=>{},[searchTerm]);) is not inside a function component at the top level
    }

    return (
        <div>
            <p>{searchTerm}</p>
        </div>
    );
}
